# Experiment Guide

Run the deterministic smoke suite with `python experiments/run_all.py --seed 42`. It generates a synthetic retail family and writes measured output to `outputs/experiments/smoke_results.json`. It is a smoke check, not a publication result.
