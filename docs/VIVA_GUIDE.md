# Viva Guide

AURA-BI autonomously interprets business schemas, selects deterministic analytics, stores traceable evidence, and optionally uses Gemini only to explain computed evidence. It differs from NEXORA-CDI: AURA-BI does not estimate treatments, counterfactuals, calibrated decision confidence, decision gates, or causal provenance.
