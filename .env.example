# SQLite remains the local default. Use PostgreSQL in production.
DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/aura_bi
# Optional: only enables evidence narration when true.
AURA_ENABLE_LLM=false
GEMINI_API_KEY=your_optional_key
